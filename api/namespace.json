---
---
{
{% for page in site.namespace %}"{{ page.slug }}": {
  "slug": "{{ page.slug }}",
  "issue": "{{ page.issue }}",
  "description": "{{ page.description }}",
  "owner": "{{ page.owner }}"
}{% unless forloop.last %},{% endunless %}
{% endfor %}
}
