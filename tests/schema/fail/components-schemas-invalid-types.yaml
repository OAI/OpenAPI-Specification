openapi: 3.2.0

# this example shows invalid types for the schemaObject

info:
  title: API
  version: 1.0.0
components:
  schemas: # must all be objects
    invalid_null: null
    invalid_number: 0
    invalid_array: []

