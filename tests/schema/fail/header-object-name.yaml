openapi: 3.3.0
info:
  title: the header name, as used in serialization, has a constrained syntax
  version: 1.0.0
paths:
  /foo:
    get:
      responses:
        default:
          headers:
            'Bad=Header':
              schema: {}
