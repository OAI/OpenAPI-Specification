openapi: 3.1.0
info:
  title: API
  version: 1.0.0
servers:
  - url: true # not a string
    description: true # not a string
    name: true # not a string
    variables: [] # not an object
  - description: no url
    variables:
      no-object: true
      invalid-enum:
        enum: true
        description: true
      invalid-enum-value:
        enum: [42]
      invalid-default:
        default: true
