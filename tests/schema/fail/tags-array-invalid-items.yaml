openapi: 3.1.0
info:
  title: API
  version: 1.0.0
tags:
  - true                  # must be an object
  - description: no name  # object must have `name` field
  - name: true            # must be a string
    description: true     # must be a string
    externalDocs: true    # must be an object
  - name: foo
    externalDocs:
      url: true           # must be a string
  - name: bar
    externalDocs:         # object must have `url` field
      description: true   # must be a string
