openapi: 3.2.0
info:
  title: API
  version: 1.0.0
components:
  schemas:
    List:
      type: array
      xml:
        wrapped: true       # can't have both `wrapped` and `nodeType`
        nodeType: element
