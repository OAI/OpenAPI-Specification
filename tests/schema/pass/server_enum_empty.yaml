openapi: 3.0.0

# the server variable enum SHOULD contain the default value, but it doesn't have to

info:
  title: API
  version: 1.0.0
servers:
  - url: https://example.com/{var}
    variables:
      var:
        enum: []
        default: a
paths: {}
